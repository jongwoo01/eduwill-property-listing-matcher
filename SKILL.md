---
name: eduwill-property-listing-matcher
description: >-
  공인중개사의 매물장을 Google Sheets 또는 로컬 Excel(.xlsx)로 만들거나 연결하고, 매물을 추가·수정·완료 처리하며,
  손님 조건으로 검색해 검증된 후보와 확인 필요 후보를 구분한다. 사용자가 매물장 생성·연결·전환, 매물 등록·변경,
  조건 검색, 손님용 매물 안내를 요청할 때 사용한다. 시세·광고 문구·계약서·법령·세금만 요청하면 사용하지 않는다.
---

# 매물 매칭

운영 원본은 Google Sheets 또는 로컬 Excel 하나다. 요청을 먼저 분류한 뒤 연결을 확인한다.

## 1. 의도부터 분류

1. **새 매물장 생성**: 사용자가 지정한 저장 방식이 활성 프로필보다 우선한다.
   - “새 Excel 매물장”이면 Google 도구를 호출하지 않는다. 폴더가 없을 때만 폴더를 한 번 묻고 빈 표준 매물장을 만든다.
   - “새 Google 매물장”이면 Google 연결 게이트를 통과한 뒤 새 시트를 만든다.
2. **기존 매물장 전환·연결**: `profile list`로 기존 프로필을 찾거나 사용자가 준 파일·시트 링크를 검증한 뒤 활성화한다.
3. **현재 매물장 작업**: 그때만 `profile show`로 활성 프로필을 읽고 저장 방식별 게이트를 실행한다.

프로필이 없고 방식도 말하지 않았다면 `references/setup.md`에 따라 Google Sheets와 Excel 중 하나만 선택받는다.

## 2. 연결 게이트

### Google Sheets

`references/google-sheets-workflow.md`를 읽고 다음을 모두 확인한다.

1. Google Drive 플러그인과 `get_profile`, `get_spreadsheet_metadata`, `get_spreadsheet_range` 또는
   `get_spreadsheet_cells`, `import_spreadsheet`, `batch_update_spreadsheet` 도구가 사용 가능하다.
2. `get_profile`의 현재 계정이 저장된 `account`와 같다.
3. 메타데이터 응답의 스프레드시트 ID가 저장된 `sheet_id`와 같다.
4. 저장된 `listing_sheet`·`detail_sheet` 탭과 표준 헤더가 실제로 읽힌다.

429, 5xx, 타임아웃만 한 번 재시도한다. 도구 없음, 인증·권한, 파일 없음, 계정·ID 불일치는 재시도하지 않는다.
실패 시 Excel로 자동 전환하지 말고 실패 지점, 기존 원본 미변경 여부, 사용자가 할 다음 행동 하나만 안내한다.

### 로컬 Excel

Google 플러그인은 필요 없다. `profile show`가 저장된 절대경로의 파일, `매물`·`매물상세` 탭과 헤더를 검증한다.
파일이 이동·삭제됐거나 헤더가 다르면 빈 파일을 만들지 말고 정확한 파일 경로를 다시 받는다.

## 3. 안전 불변식

- 검색은 읽기 전용이다. 변경은 대상과 전후 값을 확인받은 뒤 수행한다.
- 추가 전 번호·중복 후보를 검사하고, 쓰기 후 해당 셀을 재조회한다. 확인 생략 요청도 이 검증은 생략하지 않는다.
- 기존 원본·프로필·행은 삭제하거나 덮어쓰지 않는다. 종료는 `상태=완료`, 일시 제외는 `상태=보류`다.
- 모르는 값은 `?`, 해당 없는 가격은 빈칸이다. `종류·거래·지역` 중 하나라도 미확인이면 `상태=보류`로 강제한다.
- 값이 채워져도 자동으로 진행으로 바꾸지 않는다. 사용자가 명시적으로 진행 처리할 때만 바꾼다.
- 개인정보는 `매물상세`에만 둔다. `매물상세` 번호는 같은 파일의 `매물` 번호에 반드시 존재해야 한다.
- Google은 대상 범위만 배치 변경하고 재조회한다. Excel은 해시·백업·원자 교체와 셀 단위 변경을 사용한다.
- `needs_verification`은 적합 건수에 포함하지 않고, 요청 시에만 별도 확인 필요 후보로 보여준다.
- 번호 없이 수정·완료를 요청하면 지역·단지·동호·가격 등으로 한 건을 식별한다. 후보가 둘 이상이면 쓰지 말고 선택받는다.

## 4. 사용자 응답 계약

- 쓰기 전: 운영 원본과 대상 번호, 바뀌는 값만 보여주고 확인받는다.
- 성공: 처리 건수와 번호, 실제 변경값, 보류·중복 경고, 운영 원본 링크 또는 파일, 다음 행동 하나를 말한다.
- 실패: 실패 단계, 원본이 변경되지 않았는지 여부, 사용자가 할 다음 행동 하나를 말한다.
- 부분 성공: 재조회로 확인된 반영 번호와 미반영 번호를 나누고, 같은 변경을 새 번호로 다시 만들지 않는다.
- 내부 명령·JSON·셀 범위보다 업무 결과를 먼저 말한다.

중복 후보가 있으면 저장 사실과 겹친 기존 번호를 알리고 `둘 다 유지`, `두 번호 비교`, `하나를 보류` 중 하나를 고르게 한다.

## 5. 작업별 문서

| 작업 | 읽을 문서 |
| --- | --- |
| 최초 설정·새 매물장·전환 | `references/setup.md` + 선택한 저장 방식 문서 |
| Google 읽기·쓰기·복구 | `references/google-sheets-workflow.md` |
| Excel 생성·읽기·쓰기·복구 | `references/excel-workflow.md` |
| 조건 해석·검색·정렬 | `references/matching-rules.md` |
| 손님에게 보낼 문안 | `references/client-message.md` |
| CSV·PDF·채팅 자료 이관 | `references/import-workflow.md` + 선택한 저장 방식 문서 |
| 헤더·값 규칙 | `references/sheet-schema.md` |
| 로컬 CLI | `references/tool-usage.md` |

한 요청에서 필요한 문서만 읽는다. 검색 결과의 `warnings`는 사용자에게 전달하며, 거래유형이 둘 이상이면 유형별로 나누고
유형 간 가격 순위를 만들지 않는다.
